# Historical Demo Scripts

This directory contains older presentation-specific helpers.

These scripts are not the canonical runtime path for the current project.

Use the active scripts instead:
- `scripts/live-pipeline-log.sh --attacks`
- `scripts/tail-pipeline-pods.sh`
- `scripts/run-live-attacks.sh`
- `scripts/pre-demo-check.sh`
- `scripts/demo-readiness.sh --quick`

Keep this folder as historical material only unless a specific older demo flow is being reproduced deliberately.
