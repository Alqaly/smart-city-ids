apiVersion: v1
kind: Service
metadata:
  name: ids-operator-metrics
  namespace: smart-city
  labels:
    app: ids-operator
spec:
  type: ClusterIP
  selector:
    app: ids-operator
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001
