apiVersion: v1
kind: Service
metadata:
  name: iot-device-metrics
  namespace: smart-city
  labels:
    app: iot-device
spec:
  selector:
    app: iot-device-enhanced
  ports:
  - name: metrics
    port: 5000
    targetPort: 5000
    protocol: TCP
  type: ClusterIP
  clusterIP: None